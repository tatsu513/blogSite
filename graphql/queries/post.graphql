query postPage($id: ID!) {
  post(id: $id) {
    id
    title
    content
    date
    featuredImage {
      node {
        mediaItemUrl
      }
    }
    categories {
      nodes {
        name
      }
    }
  }
}
